# test

## Retry backoff constants

Use the following retry backoff constants as the canonical values:

- `initialBackoff`: `250ms`
- `maxBackoff`: `4s`
- `backoffMultiplier`: `2`
- `maxRetries`: `5`

## hello1 parallel spec (inlined)

The equivalent hello1 parallel spec section is inlined here for quick review:

```text
Retry backoff constants (hello1):
- initialBackoff: 250ms
- maxBackoff: 4s
- backoffMultiplier: 2
- maxRetries: 5
```
